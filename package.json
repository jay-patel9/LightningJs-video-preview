{
  "name": "com.domain.app.VideoPreviews",
  "description": "VideoPreviews",
  "dependencies": {
    "@lightningjs/cli": "^2.9.1",
    "@lightningjs/sdk": "^5.2.0",
    "@lightningjs/ui": "^1.3.1",
    "esbuild": "^0.15.17",
    "wait-on": "^7.0.1"
  },
  "scripts": {
    "cypress": "cypress open",
    "start": "lng build & lng serve & wait-on http://localhost:8080 & npm run cypress:run & lng dist",
    "cypress:run": "cypress run --headless"
  },
  "devDependencies": {
    "cypress": "^12.8.1"
  }
}
